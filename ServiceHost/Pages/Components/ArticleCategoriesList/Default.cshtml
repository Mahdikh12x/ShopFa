@model System.Collections.Generic.List<_01_ShopFaQuery.Contracts.ArticleCategory.ArticleCategoryQueryModel>

<div class="single-sidebar-widget">

    <h4 class="single-sidebar-widget__title">دسته بندی مقالات</h4>
    <ul class="single-sidebar-widget__category-list">
        @foreach (var category in Model)
        {
            <li class="has-children">
                <a asp-page="/ArticleCategory" asp-route-id="@category.Slug" class="  ">@category.Name<span class="counter">@category.ArticleCount</span></a>
            </li>
            @*@(Model.First() == category ? "active" : "")  *@
        }
    </ul>
</div>
